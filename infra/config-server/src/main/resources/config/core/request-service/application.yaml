server:
  port: 0

spring:
  application:
    name: request-service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:25434/requests
    username: requests
    password: requests

stats-server:
  url: http://localhost:9090
